<div id="new_tweet">
  <%= form_for @tweet, remote: true do |f| %>
    <%= f.label :tweet %> <br>
    <%= f.text_area :tweet %><br>
    <%= f.hidden_field :user_id, :value => session[:user_id] %><br>
    <%= f.submit(:class => 'sign_up_btn') %>
  <% end %>
</div>
